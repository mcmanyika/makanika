# Copy to functions/.env for local emulator (gitignored).
# For production: set the same keys on deployed Cloud Functions, then redeploy.

STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Public URL of your Next.js app (no trailing slash). Use production URL in functions/.env.
# For local emulator, override in functions/.env.local (not deployed).
APP_URL=https://makanika-oqw5.vercel.app
